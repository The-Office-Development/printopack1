# local secrets for wrangler pages dev (never commit)
.dev.vars
.env

# private handoff / internal brief: commercial strategy + credential notes. NEVER commit.
CLAUDE.md

# dependencies / build
node_modules/
dist/
.astro/
.DS_Store
*.log
npm-debug.log*

# Local content reference scraped from the live site (not shipped)
reference/
.wrangler
node_modules
dist

# Pictures the client uploaded, written into the build workspace by scripts/fetch-content.mjs.
# They live in D1, not in git.
public/uploads/
